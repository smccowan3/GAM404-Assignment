using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterRoster : MonoBehaviour
{
    public List<Character> CharList = new List<Character>(15);
    // Start is called before the first frame update
    void Start()
    {
    }
}
